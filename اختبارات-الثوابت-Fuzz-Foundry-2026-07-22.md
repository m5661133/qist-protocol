# تقرير Invariant + Fuzz Testing — MurabahaV6

**التاريخ:** 2026-07-22
**الأداة:** Foundry (forge 1.7.1) — طبقة اختبار فوق مشروع Hardhat القائم (لا تحلّ محله)
**الملفات:** `foundry.toml` · `test/foundry/Handler.sol` · `test/foundry/MurabahaInvariant.t.sol`
**الحملة:** 150 مسار × 120 عمق = **18,000 نداء عشوائي لكل ثابت**

---

## لماذا هذه الطبقة؟

التحليل الساكن (Slither/Aderyn) يفحص **النمط**؛ الـ invariant testing يفحص **السلوك الديناميكي**: يولّد آلاف تسلسلات الاستدعاءات العشوائية (إنشاء عرض → شراء → دفع → سداد مبكر → إضافة/سحب ضمان → تحريك السعر → تصفية) ويتأكد أن خصائص أمنية أساسية **لا تنكسر أبداً** مهما كان الترتيب أو المدخلات. هذا أقوى دليل جاهزية قبل التدقيق، ويرفع ثقة المدقّق.

**عالم الاختبار:** بيع + ضمان = WBTC، دفع = USDC (يغطّي محرّك المحاسبة الكامل ويتجنّب تعقيد ETH push/pull — يُغطّى منفصلاً في اختبارات Hardhat).

---

## الثوابت المُثبَتة (6) — كلها ✅ عبر 18,000 نداء

| # | الثابت | الخاصية المحمية |
|---|--------|-----------------|
| **I1** | `wbtcSolvency` | رصيد WBTC للعقد ≥ (مخزون العروض الباقي + ضمانات المراكز المقفلة). **الملاءة** — العقد لا يصبح معسراً أبداً. |
| **I2** | `noUsdcHeld` | رصيد USDC للعقد = **0** دائماً. الأقساط تُمرَّر فوراً للبائع → لا احتجاز أموال. |
| **I3** | `noPendingEth` | `totalPendingETH` = 0 ورصيد ETH = 0 (عالم WBTC/USDC بحت — يكشف أي تسرّب محاسبي). |
| **I4** | `installmentIntegrity` | `paidInstallments ≤ totalInstallments` لكل مركز — لا سداد زائد. |
| **I5** | `offerIntegrity` | `saleAmount ≤ totalAmount` لكل عرض — لا خلق مخزون. |
| **I6** | `settlementConservation` | كل تصفية تُصفّر الضمان وتضبط الحالة LIQUIDATED — لا خلق/إتلاف قيمة عند التسوية. |

> **ملاحظة منهجية:** فحص التسوية (I6) مبنيّ كـ**علم يُرفَع في الـHandler ويُفحَص في طبقة الثابت** بدل `assert` داخلي — كي لا يُبتلع تحت `fail_on_revert=false`. هذا يمنع الإيجابيات الكاذبة الخفية.

---

## تغطية الحملة (عيّنة مسار واحد)

```
createOffer: 14   buy: 12   payInstallment: 3   earlyRepay: 3   liquidate: 4   settlementChecks: 4
```
كل الأفعال الحرجة نُفّذت فعلاً (ليست حملة فارغة). الانحرافات: 3-4 ارتدادات/18,000 نداء = حواف `bound` في الـHandler، **لا تؤثر على الثوابت** (النداء المرتدّ تُلغى آثاره ويُفحَص الثابت على الحالة السابقة السليمة).

---

## اختبارات دورة حياة محدّدة (إثبات الميكانيكا) — ✅

- `test_lifecycle_createBuyPayComplete`: إنشاء عرض → شراء بضمان → دفع 3 أقساط → اكتمال. يؤكّد `paid==total` و`USDC==0`.
- `test_lifecycle_liquidation`: تجاوز مهلة السماح (GRACE) → `isLiquidatable==true` → تصفية → ضمان مصفّر + حالة LIQUIDATED.

---

## النتيجة

**صفر كسر ثابت عبر 18,000 نداء × 6 ثوابت + دورتا حياة.** المحاسبة الأساسية (الملاءة، عدم احتجاز الأموال، حفظ القيمة عند التسوية) صامدة ديناميكياً.

## الأوامر

```bash
export PATH="$HOME/.foundry/bin:$PATH"
forge test --match-contract MurabahaInvariant -vv        # الحملة + دورات الحياة
# حملة أعمق (ثقة أعلى قبل الإطلاق):
forge test --match-contract MurabahaInvariant \
  --fuzz-runs 500 --show-progress -vv
```

## فجوات مقصودة (للتدقيق الخارجي أو توسّع لاحق)

- مسارات ETH (بيع/ضمان address(0)) وpull/push المعلّق — مغطّاة في Hardhat، لم تُدرَج في هذه الحملة.
- مسارات الأتمتة (autoPay/autoLiquidate + رسومها + keeper) — `enableAutoPay=false` هنا؛ تستحق ثابتاً لرصيد رسوم البروتوكول.
- تعدّد رموز البيع/الضمان في آنٍ (WBTC فقط هنا).

هذه ليست نواقص أمنية بل حدود نطاق الحملة الحالية — تُوسَّع بسهولة بإضافة أفعال للـHandler.
