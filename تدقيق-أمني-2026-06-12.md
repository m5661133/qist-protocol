# تقرير تدقيق أمني — MurabahaV6 (Base Mainnet)
**التاريخ:** 2026-06-12 · **المنهج:** 3 وكلاء متوازين (ترقية/صلاحيات · إعادة دخول/تحويلات · حساب/أوراكل/تصفية) + تحقّق المنسّق من كل ادّعاء مقابل الكود الفعلي.

> ⚠️ هذا تدقيق آلي مساعد، لا يُغني عن تدقيق احترافي مدفوع قبل التوسّع العام. الخطورات أدناه **مُعايَرة يدوياً** بعد تصحيح مبالغات الوكلاء.

---

## 🔴 يجب إصلاحه قبل أي استخدام عام (مؤكَّد مقابل الكود)

### H1 — `GRACE_PERIOD` ما زالت قيمة الاختبار (180 ثانية) على العقد الحيّ
**`MurabahaV6.sol:53`** → `uint256 public constant GRACE_PERIOD = 3 minutes; // 180s — للاختبار (كان 3 أيام للإنتاج)`
- التعليق نفسه يقرّ أنها قيمة اختبار. النتيجة: مركز يتأخّر **3 دقائق فقط** بعد موعد القسط يصبح قابلاً للتصفية → تصفية ظالمة لأي تأخّر شبكي بسيط.
- **الإصلاح:** رفعها إلى `3 days` (259200). بما أنها `constant` فالتغيير يتطلّب **ترقية implementation** عبر المسار الآمن.

### H2 — لا فحص Chainlink L2 Sequencer Uptime Feed
**`PriceLib.sol:27-40` + `MurabahaV6.sol:713-727`** (تأكّد: لا أثر لـ sequencer في كل `contracts/`؛ PriceLib يتجاهل `startedAt` صراحةً سطر 28)
- العقد على Base (L2). عند توقّف الـ sequencer ثم عودته، يُعاد آخر سعر «متجمّد» بـ `updatedAt` قد يمرّ فحص العمر (ساعة) لكنه لا يعكس حركة السوق أثناء التوقّف → تسعير/تصفية على سعر خاطئ (أشهر استغلال موثّق على L2).
- **الإصلاح:** فحص Sequencer Uptime Feed لـ Base (`0xBCF85224fc0756B9Fa45aA7892530B47e10b6433`) داخل `priceUSDC` قبل الاعتماد على السعر:
```solidity
(, int256 up, uint256 startedAt,,) = sequencerFeed.latestRoundData();
if (up != 0) revert SequencerDown();                       // 1 = down
if (block.timestamp - startedAt <= 3600) revert GracePeriodNotOver();
```
يتطلّب ترقية.

---

## 🟡 يُفضّل إصلاحه / تحصين

| # | الملاحظة | الموضع | الإصلاح |
|---|----------|--------|---------|
| M1 | الترقية بيد **EOA مفرد** بلا Timelock/Multisig → اختراق المفتاح = خسارة كاملة | `_authorizeUpgrade` (225) + المالك | نقل الملكية إلى Gnosis Safe خلف TimelockController (بلا تغيير كود) |
| M2 | هامش تصفية ضيق (عتبة 105٪) لأصل متقلّب → خطر دين معدوم في الهبوط السريع | `LIQUIDATION_THRESHOLD_BPS=10500` (46) | توسيع العتبة و/أو تقليل فاصل keeper و/أو تفعيل auto-liquidate افتراضياً |
| M3 | توكنات fee-on-transfer / rebasing تكسر المحاسبة **إن أُضيفت** | `_buy`/`addCollateral` (416/531) | منعها بسياسة `addSupportedToken`، أو قياس الرصيد المستلم فعلياً |
| M4 | `upgrade_base.ts` يستدعي `upgradeToAndCall` يدوياً → يتخطّى فحص توافق التخزين | `scripts/upgrade_base.ts` | استخدام `upgrades.upgradeProxy(BASE_PROXY, Factory, {kind:"uups"})` دائماً |
| M5 | `initializeV2()` بلا `onlyOwner` (قابلة للـ front-run) — بلا أثر مالي لكن تحرق درجة reinitializer | `MurabahaV6.sol:214` | إضافة `onlyOwner` أو حذفها إن لم تَعُد لازمة |

---

## 🟢 منخفض / معلومة (صحّحتُ مبالغة الوكلاء)

- **`__gap` غائب** — الوكيل صنّفها «حرج»، **والصحيح منخفض**: هذا العقد ورقة نهائية، وإلحاق متغيّرات جديدة في نهايته آمن بدون gap. يصبح مهمّاً فقط لو أُدرج عقد أب جديد. *(يُستحسن إضافته احترازاً عند الترقية القادمة.)*
- **القسط = 0 لمراكز الغبار** — الوكيل صنّفها «حرج»، **والصحيح منخفض**: المشتري يدفع كامل `totalPayable` في القسط الأخير (لا يأخذ الضمان مجاناً). الأثر الفعلي محصور في مراكز ضئيلة جداً + تقديم جدول بتكلفة صفر (محدود بعدد الأقساط). *(يُستحسن حدّ أدنى للشراء/القسط في `_buy`.)*
- **`ReentrancyGuard` غير القابل للترقية** — خلاف بين الوكيلين، **حُسم من المصدر**: في OZ **5.6.1** يستخدم فتحة ERC-7201 منعزلة (`0x9b779b…`)، فهو **آمن تخزينياً ووظيفياً** خلف الـ Proxy (و108 اختبار تؤكّده). الوكيل 1 محقّ. *(اختياري: التوحيد على `ReentrancyGuardUpgradeable` للوضوح.)*
- **التقريب** ينحاز دائماً للمشتري بمقدار wei (لا overflow، اتجاه آمن).

---

## ✅ نقاط قوة مؤكَّدة (العقد مبنيّ جيداً في جوهره)
- نمط Checks-Effects-Interactions **صحيح في كل مسارات التحويل**.
- تغطية `nonReentrant` **شاملة** على كل دالة تنقل أصولاً (14 دالة) — لا التفاف ولا cross-function.
- `SafeERC20` في كل تحويلات ERC20 (يدعم USDT بلا قيمة إرجاع).
- ETH بنمط **pull** (`pendingETH`/`withdrawETH`) محصّن ضد DoS.
- التحكم بالصلاحيات **سليم** (جُرِدت 21 دالة — لا حسّاسة مكشوفة)، سقوف رسوم، منع Self-Buy.
- `_disableInitializers()` في الـ constructor → لا هجوم implementation غير مُهيّأ.
- تطبيع decimals ديناميكي من الـ feed، والقسط الأخير يبتلع البقايا (لا غبار wei).

---

## ترتيب العمل الموصى به
1. **قبل أي استخدام عام:** ترقية واحدة تجمع **H1 (GRACE→3 أيام)** + **H2 (فحص sequencer)**.
2. **قريباً:** نقل الملكية إلى **Safe + Timelock** (M1) — بلا كود.
3. **تحصين:** حدّ أدنى للشراء (القسط=0)، سياسة توكنات (M3)، إصلاح سكربت الترقية (M4)، حماية `initializeV2` (M5)، توسيع هامش التصفية (M2).
