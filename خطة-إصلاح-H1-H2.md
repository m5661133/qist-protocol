# خطة إصلاح H1 + H2 — MurabahaV6 (تدريب: Plan Mode + TDD)
**التاريخ:** 2026-06-12 · **النطاق:** كود + اختبارات فقط — **لا نشر على Mainnet** (مؤجّل لقرار المالك قبل الإطلاق).

## الهدف
1. **H1** — رفع `GRACE_PERIOD` من 180s إلى 3 أيام (259200) — *في الكود؛ يُفعَّل على الحيّ عند الترقية فقط*.
2. **H2** — إضافة فحص Chainlink L2 Sequencer Uptime Feed قبل الاعتماد على السعر، بتصميم يعمل على كل الشبكات.

## التصميم (مُعتمَد)
- `GRACE_PERIOD = 3 days` (ثابت، بلا تغيير تخزين).
- `address public sequencerUptimeFeed;` — **يُلحَق في نهاية التخزين** بعد `_pendingETH` (إلحاق آمن UUPS).
- `setSequencerUptimeFeed(address) onlyOwner` — يُضبط بعد الترقية (افتراضياً 0 = الفحص متخطّى).
- `PriceLib.requireSequencerUp(IChainlinkFeed seq, uint256 grace)` — يرفض إن `up != 0` أو ضمن مهلة تعافٍ.
- `SEQUENCER_GRACE_PERIOD = 1 hours` (ثابت).
- يُستدعى الفحص في `_tokenPriceUSDC` و`_tokenValueUSDC` للأصول غير المستقرة فقط، وفقط إذا `sequencerUptimeFeed != address(0)`.
- Mock جديد `MockSequencerFeed` في `Mocks.sol` للاختبار.

## خطوات TDD (Red → Green → Refactor)
1. **🔴 Red:** اختبارات فاشلة:
   - sequencer متوقّف (`up=1`) → `priceUSDC`/الشراء يرفض.
   - عاد حديثاً (ضمن `SEQUENCER_GRACE`) → يرفض.
   - عاد وتجاوز المهلة → يقبل.
   - `sequencerUptimeFeed` غير مضبوط → لا فحص (سلوك حالي محفوظ).
   - تصفية overdue مع `GRACE_PERIOD = 3 days` (تحديث ثابت `GRACE` في الاختبارات).
2. **🟢 Green:** تنفيذ التعديلات (تُعرض على المالك قبل تطبيق `.sol`).
3. **♻️ Refactor + تحقّق:** السويت كاملة خضراء (متوقّع > 108)، + `npx hardhat compile`.

## التسليم
- تعديلات كود مراجَعة + **سكربت ترقية آمن** يستخدم `upgrades.upgradeProxy`/`validateUpgrade` (يعالج أيضاً M4).
- المالك يقرّر النشر ووقته، ويضبط عنوان feed سيكوينسر Base حينها (يُؤكَّد من توثيق Chainlink).

## خارج النطاق (لا تفعله الآن)
- النشر/الترقية على Mainnet · تغيير العقد الحيّ · بقية ملاحظات التدقيق (M1–M5) — لاحقاً.
