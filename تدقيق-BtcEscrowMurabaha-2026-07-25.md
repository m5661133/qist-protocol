# تقرير تدقيق أمني — BtcEscrowMurabaha

**التاريخ:** 2026-07-25 · **النطاق:** `contracts/BtcEscrowMurabaha.sol` (بيع مرابحة بضمان بيتكوين حقيقي)
**النوع:** تدقيق ذاتي قبل النشر (دفاع متعدد الطبقات) — **لا يُغني عن تدقيق خارجي قبل Mainnet.**

## المنهجية
1. تحليل ساكن: **Slither 0.11.5** (101 كاشف) + **Aderyn 0.6.8** (88 كاشف).
2. مراجعة يدوية وفق قائمة الثغرات (reentrancy، صلاحيات، Oracle، proxy، DoS، front-running).
3. اختبارات: **28 اختبار Hardhat** (كل الانتقالات والحمايات) + اختبار Fork ضد Chainlink الحقيقي على Base + اختبار تكامل حقيقي على Regtest.

## النتائج والإصلاحات

| # | الشدّة | SWC | الوصف | الحالة |
|---|---|---|---|---|
| H-1 | High* | SWC-132 | قفل ETH بلا دالة سحب | ✅ **أُصلح** — فرع استرجاع ETH في `emergencyWithdraw` (العقد أصلاً بلا payable/receive — إيجابية كاذبة عملياً) |
| L-6 | Low | — | `nonReentrant` ليس المُعدِّل الأول في `payInstallment` | ✅ **أُصلح** — أصبح أولاً |
| L-12 | Low | SWC-131 | خطأ `NotSeller` + مُعدِّل `onlySeller` كود ميت | ✅ **أُصلح** — حُذفا |

## نتائج مقبولة بالتصميم (لا تتطلّب إصلاحاً)

| النتيجة | المبرّر |
|---|---|
| `timestamp` (SWC-116) | المهل بالأيام (GRACE=3، DISPUTE=2، DELIVERY=2)؛ تلاعب المدقّق بثوانٍ لا يؤثر — نفس نمط MurabahaV6 |
| `low-level-call` | استرجاع ETH عبر `.call` — محمي بـ`onlyOwner`+`whenPaused` وفحص العائد |
| Centralization (L-1) | دوال المالك = **Safe 2-of-3**؛ صلاحيات إدارية موثّقة، لا تمسّ أموال المستخدمين (BE-07) |
| Empty Block (L-2) | `_authorizeUpgrade` — نمط UUPS القياسي |
| `__gap` unused (L-14) | فراغ تخزين مقصود لترقيات UUPS |
| `^0.8.22` (L-11) | اصطلاح المشروع (يطابق MurabahaV6) |

## مراجعة يدوية — النقاط الرئيسية ✅
- **Reentrancy:** `payInstallment` يتبع checks-effects-interactions + `nonReentrant`؛ USDC موثوق (لا callback).
- **الصلاحيات:** كل دالة مقيّدة بدورها (buyer/attestor/arbiter/owner)؛ `attestor` يسجّل فقط ولا يحرّك BTC.
- **Oracle:** Chainlink BTC/USD + فحص Sequencer + staleness (`PriceLib`)؛ قسمة LTV محميّة من الصفر.
- **البيع لا القرض:** المشتري لا يستلم USDC (BE-08)؛ العقد لا يحتجز USDC (تُمرّر لحظياً).
- **الذرّية:** العقد لا يتقدّم إلا بإيداع الطرفين؛ الإفراج مشروط بـ`REPAID` أونشين (BE-05).
- **DoS:** لا حلقات غير محدودة. **Front-running:** قبول العرض تنافسي لكن مقيّد بمهلة تسليم + إلغاء.

## المخاطر المتبقّية (تتطلّب انتباهاً)
1. **مركزية `attestor`/`arbiter`** — يُنصح بترقيتهما إلى multisig/HSM قبل مبالغ كبيرة (Threat Model §6).
2. **ترقية UUPS** — المالك (Safe) يستطيع تغيير المنطق؛ يُنصح بـTimelock للترقيات.
3. **عدم الذرّية بين Base وBitcoin** — مُدار بربط الحالات بالأحداث والمهل (مقبول بالتصميم).

## البوابة
- التحليل الساكن: **لا نتائج عالية/متوسطة أمنية حقيقية.** ✅
- الاختبارات: **28/28 + Fork + regtest e2e.** ✅
- **النتيجة: PASS للتدقيق الذاتي** — يسمح بالنشر على **Testnet**.
- ⛔ **قبل Mainnet:** تدقيق خارجي مستقل + بوابة `qist-pre-upgrade` (استراتيجية D-057).

> يُوصى بإضافة اختبارات Foundry invariant (handler + ghost variables) للرياضيات المالية قبل التدقيق الخارجي.
