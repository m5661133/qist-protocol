# AI_RULES.md — العقد الذكي (Qist Protocol)
> قواعد عمل دائمة لأي وكيل AI على هذا المشروع.
> تُطبَّق بجانب `AI_HANDOFF.md` لا بديلاً عنه — اقرأ الاثنين قبل أي تعديل.
> ⚠️ هذا المشروع فيه عقود **منشورة حيّاً على Base Mainnet بأموال حقيقية**. الخطأ هنا يكلّف مالاً.

## 1. افهم الكود الحالي قبل التعديل
اقرأ الملف كاملاً (لا مقتطفاً) قبل تعديله، وافهم من يستدعيه ومن يعتمد عليه.

## 2. لا تغيّر البنية المعمارية بدون سبب واضح وموثَّق
لا تُعِد الهيكلة ولا تنقل منطقاً من طبقة لأخرى إلا بطلب صريح من المستخدم أو بحاجة واضحة موثَّقة.
أي قرار معماري كبير → توقّف واشرحه أولاً (القاعدة 14).

## 3. لا تغيّر الواجهات العامة (public interfaces) بدون ضرورة
أي توقيع دالة أو حقل بنية تُقرأ من أماكن متعددة يحتاج تحديثاً متزامناً لكل نقاط الاستدعاء.

## 4. لا تحذف وظيفة قائمة لإصلاح مشكلة أخرى
أضِف بدل أن تستبدل، إلا إذا كان الحذف نفسه هو المهمة المطلوبة صراحة.

## 5. أصلح جذر السبب بدلاً من التفافة (workaround) متى أمكن
إن لم يمكن الوصول لجذر السبب بثقة، قل ذلك صراحة بدل تقديم إصلاح سطحي كأنه نهائي.

## 6. لا تعدّل ملفات غير مرتبطة بالمهمة
لا تُصلح تحذيرات قديمة ولا تُعِد تنسيق ملفات لم تطلب مهمتك تعديلها، حتى لو بدت «تحسينات بديهية».

## 7. اعمل أقل تغيير ممكن
غيّر أقل عدد أسطر يحقق الهدف. لا إعادة كتابة ملف كامل إن كفى تعديل جزئي.

## 8. حافظ على التوافق الرجعي إذا كان مطلوباً
أي تغيير كاسر يحتاج تأكيداً صريحاً من المستخدم بعد شرح العواقب.

## 9. لا تعرض ولا تسجّل ولا تلتزم (commit) أي أسرار
لا مفاتيح خاصة، لا seed phrases، لا مفاتيح API — ولا حتى في رسائل سجل أو نص تشخيصي مؤقت.

## 10. لا تعدّل `.env` بقيم حساسة
ولا تكتب قيماً فعلية في أي ملف تسليم أو تقرير.

## 11. شغّل الاختبارات المناسبة بعد كل تعديل
(الأوامر الدقيقة في `AI_HANDOFF.md` ← قسم Test Commands.)

## 12. شغّل البناء/الفحص الساكن (lint/typecheck) قبل اعتبار المهمة مكتملة
أي تحذير **جديد** في ملف عدّلته غير مقبول؛ التحذيرات القديمة غير المرتبطة مقبولة.

## 13. لا تدّعِ نجاح المهمة بدون تحقّق فعلي
«نُفِّذ الأمر بلا خطأ ظاهر» ليس تحققاً. استند لمخرجات حقيقية.

## 14. إن وجدت قراراً معمارياً كبيراً مطلوباً، توقّف واشرحه قبل تنفيذه
اشرح البديل والتكلفة والمخاطر، وانتظر قراراً صريحاً.

## 15. إن وجدت مشكلة أمنية توقّف وبلّغ عنها
لا تُصلحها بصمت ولا تتجاهلها.

## 16. لا تعمل `git push` إلا بطلب صريح من المستخدم
الالتزام المحلي مسموح ضمن تعليمات صريحة؛ الدفع للـremote يحتاج إذناً منفصلاً في كل مرة.

## 17–18. لا تستخدم أوامر git مُدمِّرة
```
git reset --hard
git clean -fd
git push --force / --force-with-lease
```
ولا أي أمر يمحو تعديلات المستخدم غير الملتزَمة — بدون موافقة صريحة لكل حالة على حدة.

## 19. بعد إكمال مهمة كبيرة حدِّث `AI_HANDOFF.md`
حدِّث تحديداً: `Current State` و`Current Task Status` (أين توقفتَ بالضبط) و`Relevant Files`
و`Known Issues`. لا تترك القارئ التالي يعتمد على معلومة تجاوزها الواقع.

## 20. اجعل الالتزامات (commits) صغيرة وواضحة
رسالة تشرح **ماذا ولماذا** بإيجاز، لا «تحديثات» عامة.

---

## قواعد إضافية خاصة بهذا المشروع

## 21. ❌ لا تستخدم وضع auto / التعديل التلقائي على أي ملف `.sol` إطلاقاً
أمر دائم وصريح من المستخدم. اقرأ الملف كاملاً وعدّله يدوياً وبحذر فقط.
لا تُشغّل أي أداة إصلاح جماعي (bulk auto-fix) على `contracts/`.

## 22. لا تلمس تخطيط التخزين (storage layout) بدون فهم كامل للأثر
العقدان يعملان بنمط **UUPS Proxy**. إضافة حقل في منتصف `struct` أو إعادة ترتيب متغيرات الحالة
تُفسد قراءة البيانات الموجودة فعلاً على السلسلة. أي تغيير في التخزين → توقّف واشرح أولاً.

## 23. قبل أي ترقية: اقرأ `deployments/btc-escrow.json` واكتب فيه القيد الجديد **قبل** التنفيذ
كل قيد يحتاج: `implementation` · `date` · `label` · `storageChange` · `rollbackSafe` · `why`.
ملاحظة موثَّقة في الملف: **الرجوع إلى v1 غير آمن** (هيكل `struct Deal` مختلف).

## 24. لا ترقية بدون اختبارات خضراء
`npx hardhat test` **و** `forge test` قبل أي `upgrade`. أي فشل = لا ترقية. بلا استثناء.

## 25. الديمون `attestor-daemon.ts` خدمة حيّة — لا توقفها ولا تعدّلها بلا انتباه
يعمل عبر LaunchAgent `info.qist.attestor` ويراقب صفقات بيتكوين حقيقية. توقفه يعني تعليق
كل حدث (إيداع/تسليم/فكّ رهن) إلى أن يُشغَّل يدوياً. سجلاته `~/Library/Logs/qist/`.
درس محفور: توقّف صامتاً ٣ أيام في أغسطس ٢٠٢٦ لأن مسار الحساب تغيّر ولم ينتبه أحد.

## 26. لا تفترض أن سكربتات `scripts/` صالحة للتشغيل
أكثر من ٦٠ سكربت وأكثرها تشخيصي لمرة واحدة (`check_pos5.ts`, `deploy_build17.ts` …).
اقرأ السكربت كاملاً وتحقّق من عناوينه وشبكته قبل تشغيله على Mainnet.

## 27. لا تلمس منطق الخزنة 2-of-3 ولا مسار الاشتقاق بلا فهم كامل
المنطق مشترك مع نواة Rust في `../qist-btc` وتطبيق Flutter في `../تطبيق الهاتف`.
`test/vault-parity.test.ts` موجود تحديداً ليحرس هذا التطابق — أي كسر له إشارة خطر لا إزعاج.

## 28. حالة git الحالية استثنائية — انتبه قبل أي أمر git
جوهر المشروع (`BtcEscrowMurabaha.sol`, `deployments/`, `foundry.toml`, `scripts/attestor-daemon.ts`)
**غير مُتتبَّع في git أصلاً**. أي `git clean` أو `git checkout .` يمحو مصدر عقد حيّ بأموال حقيقية
بلا إمكانية استرجاع. راجع القاعدتين 17–18 — هنا تحديداً ليستا نظريتين.

## 29. لا تفترض ثبات مسار المشروع بين الجلسات
هذا المشروع انتقل مساره أكثر من مرة (تغيير قرص، تغيّر اسم حساب macOS). تحقّق بـ`pwd`
و`git remote -v` بدل الوثوق بمسار مذكور في محادثة أو ذاكرة سابقة.

## 30. المسارات العربية قد تفشل بسبب ترميز Unicode (NFC/NFD)
عند فشل غامض بمسار عربي رغم صحته ظاهرياً: انسخ المسار حرفياً من مخرجات `ls`/`find`
بدل إعادة كتابته يدوياً.
