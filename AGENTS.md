# AGENTS.md — العقد الذكي

> هذا الملف يُقرأ تلقائياً من Hermes Agent في كل محادثة داخل هذا المشروع.

## أولاً: اقرأ القواعد الكاملة
اقرأ `AI_RULES.md` في جذر المشروع والتزم بكل بنوده قبل أي عمل.

## ⚠️ أعلى مستوى حساسية — عقد Solidity على Base · خطأ واحد = مال حقيقي

### ممنوع منعاً باتاً
- **لا تعديل تلقائي على أي ملف `.sol`.** القراءة والتحليل فقط ما لم يطلب المالك تعديلاً محدداً
  ويؤكّده أمام الشاشة.
- **لا نشر (deploy) ولا verify ولا أي معاملة على الشبكة** — لا على mainnet ولا testnet.
- **لا تعرض ولا تنقل** مفاتيح خاصة أو محتوى `.env` أو عبارات استرداد.
- **لا `git push`** ولا `git reset --hard`.

### إذا جاء الطلب من تيليجرام (أي محادثة خارج الطرفية)
**قراءة وتلخيص فقط.** لا تعديل ولا تنفيذ اختبارات تكتب على السلسلة. أي طلب تعديل يُردّ عليه بـ:
«العقد الذكي لا يُعدَّل من الجوال — التعديل أمام الشاشة بمراجعة يدوية.»

### الأدوات المسموحة افتراضياً هنا
قراءة ملفات · بحث · `git status` / `git log` · `forge build` و`forge test` (قراءة نتائج فقط).
