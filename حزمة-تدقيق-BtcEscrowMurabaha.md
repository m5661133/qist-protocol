# حزمة التدقيق الخارجي — نظام BtcEscrowMurabaha

> للمدقّق الخارجي. نظام بيع مرابحة إسلامي بضمان **بيتكوين حقيقي** على شبكة Bitcoin،
> مع طبقة تمويل/حالة على **Base**. غير وصائي، مقاوم لتعطّل طرف واحد.

## 1) النطاق

| المكوّن | الملف | الأسطر | الأولوية |
|---|---|---|---|
| عقد Base (المرابحة + الحالة) | `contracts/BtcEscrowMurabaha.sol` | ~620 | 🔴 حرجة |
| مكتبة الأسعار (مُعاد استخدامها) | `contracts/libraries/PriceLib.sol` | — | 🟡 (مُدقَّقة مع MurabahaV6) |
| نواة Bitcoin (PSBT/عناوين) | `qist-btc/src/{escrow,psbt}.rs` | ~500 | 🔴 حرجة |
| منطق الـattestor | `qist-btc/src/attestor.rs` · `scripts/attestor-live.ts` | — | 🟠 عالية |

**خارج النطاق:** MurabahaV6 (مُدقَّق سابقاً)، Mocks، أدوات الاختبار.

## 2) المعمارية

نظام من طبقتين **منفصلتين لا تتصلان بروتوكولياً** (لا جسر):
- **Base:** سجل شروط البيع + محرّك أقساط USDC + آلة حالات (15 حالة) + حساب LTV من Chainlink.
- **Bitcoin L1:** المبيع والرهن بيتكوين حقيقي في Multisig 2-of-3 (المشتري/البائع/قسط).
- **الربط:** خدمة `qist-btc` (attestor) تقرأ الطرفين وتنسّق. **العقد لا يملك ولا يحرّك ساتوشي.**

### تدفّق البيع (نموذج المرابحة)
```
المشتري ينشئ طلباً → البائع يقبل →
إيداع الطرفين معاً (البائع: المبيع · المشتري: الرهن) →
توقيع منسّق ذرّي (المبيع→المشتري، الرهن→2of3) →
المشتري يسدّد الثمن أقساط USDC للبائع →
سداد كامل → إفراج عن الرهن للمشتري
(تعثّر → تصفية جزئية: الدين للبائع، الفائض للمشتري)
```

## 3) نموذج الثقة والفرضيات

| الطرف | الثقة | القيد |
|---|---|---|
| المشتري/البائع | غير موثوقين | النظام يحميهما متبادلاً |
| `attestor` (قسط) | يشهد بأحداث L1 | **لا يحرّك BTC منفرداً** (مفتاح 1 من 3)؛ يسجّل فقط، لا يقفز حالات عشوائياً |
| `arbiter` (قسط) | يحسم النزاع | لا يحرّك BTC؛ لا يملك USDC |
| `owner` (Safe 2-of-3) | إدارة + ترقية | `emergencyWithdraw` يحظر USDC (BE-07) |
| Chainlink | مصدر سعر | فحص Sequencer + staleness |

**الفرضية الوحيدة غير القابلة للإزالة:** Base لا تقرأ Bitcoin، فكمية الرهن/أحداث L1 يشهد بها attestor.
تخفيفها: attestor مقيّد بالتسجيل، وتحريك BTC يتطلّب 2-of-3 حقيقية. يُنصح بجعله multisig/HSM.

## 4) الثوابت (Invariants) المطلوب التحقّق منها

1. **العقد لا يحتجز USDC** خارج نطاق `payInstallment` (تُمرَّر لحظياً): `balanceOf(contract) == 0` بين المعاملات.
2. **المشتري لا يستلم USDC إطلاقاً** (بيع لا قرض — BE-08).
3. **`sum(الأقساط المدفوعة) ≤ totalPayable`**، وآخر قسط = المتبقّي بالضبط (لا زيادة/نقص).
4. **لا تقدّم بلا إيداع الطرفين**: `AWAITING_FUNDING` تتطلّب `collateralSats>0 && merchandiseDeposited`.
5. **الإفراج مشروط أونشين**: `confirmCollateralReleased` فقط بعد `REPAID`/`CANCELLED`.
6. **التصفية مشروطة**: `confirmLiquidation` فقط في `LIQUIDATION_ELIGIBLE`.
7. **انتقالات الحالة أحادية الاتجاه** ضمن آلة الحالات (لا رجوع لحالة سابقة إلا عبر مسارات محددة).
8. **صلاحية كل دالة** مطابقة لدورها (buyer/attestor/arbiter/owner).

## 5) نتائج التدقيق الذاتي (مُصلَحة — للمراجعة)

| # | الشدّة | الوصف | الإصلاح |
|---|---|---|---|
| SR-1 | 🔴 **عالية** | **تباين بين السلسلتين**: `confirmSigned` كان يعيد فحص LTV **بعد** بثّ التوقيع على Bitcoin؛ رفضه يُحدث تبايناً (المشتري أخذ المبيع والعقد يظنّها غير مفعّلة) | أُزيل فحص LTV من `confirmSigned`؛ اللتزام يتمّ بعد الإيداع، ومخاطر السعر تُدار بالتصفية |
| H-1 | متوسطة* | قفل ETH بلا سحب (لا payable أصلاً) | فرع استرجاع ETH في `emergencyWithdraw` |
| L-6 | منخفضة | `nonReentrant` ليس أولاً | أصبح أولاً |
| L-12 | منخفضة | أخطاء/مُعدِّلات كود ميت (`onlySeller`, `InsufficientCollateral`) | حُذفت |

## 6) مناطق تركيز للمدقّق (أسئلة محدّدة)

1. **التنسيق بين السلسلتين:** هل توجد حالات تباين أخرى بين حالة العقد وواقع Bitcoin؟ (خاصة حول `confirmSigned`/`confirmLiquidation`/`confirmCollateralReleased`).
2. **صلاحية attestor:** هل يستطيع attestor خبيث إضرار طرف عبر شهادات كاذبة رغم قيد التسجيل؟
3. **LTV والتسعير:** صحّة حساب `_currentLtvBps` (خانات BTC=8, USDC=6)، وسلوك الحواف (رهن=0، سعر=0).
4. **آلة الحالات:** هل من انتقال غير مقصود؟ (خاصة `resolveDispute` الذي يسمح للمحكّم بفرض `REPAID`).
5. **التصفية:** العقد يسجّل `satsToSeller/surplus` دون التحقّق من مطابقتها للسعر — هل يكفي أن الحماية في PSBT (توقيع البائع)؟
6. **الترقية UUPS:** مخاطر `_authorizeUpgrade` (المالك Safe)؛ هل يلزم Timelock؟
7. **Griefing:** أي طرف يقبل عرضاً منشوراً؛ هل حماية المهلة+الإلغاء كافية؟
8. **رياضيات الأقساط والرسوم:** التقريب، والقسط الأخير، ورسوم البروتوكول من حصة البائع.

## 7) تغطية الاختبارات

- **28 اختبار Hardhat** (كل الانتقالات + الحمايات + الحواف).
- **اختبار Fork** ضد Chainlink BTC/USD + Sequencer الحقيقيين على Base.
- **اختبار تكامل حقيقي على Regtest** (إيداع→توقيع→إفراج، بثّ فعلي).
- **attestor حيّ** (دورة كاملة مؤتمتة بين Base وBitcoin).
- **20 اختبار Rust** (عناوين + PSBT إعادة/تصفية/توقيع منسّق + منطق attestor).
- **موصى به قبل/أثناء التدقيق:** اختبارات Foundry invariant (handler + ghost) للثوابت §4.

## 8) خطة النشر (بعد التدقيق)
Regtest ✅ → Base Sepolia → Signet → **تدقيق خارجي** → إطلاق محروس بسقوف صغيرة (D-057) → توسّع تدريجي.
النشر عبر `scripts/deploy-btc-escrow.ts` ومروراً ببوابة `qist-pre-upgrade`. المالك = Safe 2-of-3.

## 9) المخاطر المتبقّية المعلومة (مقبولة بالتصميم)
- مركزية attestor/arbiter (يُنصح multisig/HSM) · عدم الذرّية بين السلسلتين (مُدار بالمهل والحالات) ·
  ترقية UUPS (يُنصح Timelock) · تواطؤ طرفين في 2-of-3 (قيد رياضي لأي multisig بعتبة 2).

## 10) مانيفست الملفات
```
contracts/BtcEscrowMurabaha.sol            العقد
contracts/libraries/PriceLib.sol           الأسعار (مشترك)
test/BtcEscrowMurabaha.test.ts             28 اختبار
test/BtcEscrowMurabaha.fork.test.ts        اختبار Base الحقيقي
scripts/{deploy,scenario,attestor-live}-*  النشر/التجارب/الأتمتة
تدقيق-BtcEscrowMurabaha-2026-07-25.md       تقرير التدقيق الذاتي
../qist-btc/                                 خدمة Bitcoin (Rust) + THREAT_MODEL + README + db + api
```
